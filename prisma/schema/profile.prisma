model Profile {
    id      String  @id @default(uuid())
    bio     String?
    image   String?
    phone   String?
    address String?
    userId  String  @unique
    user    User    @relation(fields: [userId], references: [id] )
}
